ScrollReveal({
  origin: 'right',
  distance: '80px',
  duration: 1500,
  
}).reveal(`li , footer`, { interval: 100 })

ScrollReveal({
  origin: 'top',
  distance: '80px',
  duration: 1500,
  
}).reveal(`.container`)